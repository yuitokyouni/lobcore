# CLAUDE.md — lobcore

単一銘柄・価格時間優先の板（LOB）マッチングエンジン。
Stage 1–3 でエンジンを固め、Stage 4 で離散イベント市場シミュレータ核、Stage 5 で pybind11 の Python モジュールにする。
背景資料は docs/lobcore-context-pack.md。

## ビルドとテスト

```
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
ctest --test-dir build --output-on-failure
```

ベンチマーク（Release のみ意味がある）:

```
cmake -S . -B build-rel -DCMAKE_BUILD_TYPE=Release -DLOBCORE_BUILD_BENCH=ON
cmake --build build-rel && ./build-rel/bench/lobcore_bench
```

変更を「完了」と報告する前に、必ず Debug ビルド（ASan/UBSan 有効）で ctest 全通過を確認すること。

## 変更してはならないもの

- tests/test_book.cpp は仕様書である。テストを通すためにテスト側を書き換えない。仕様変更が必要だと考えた場合は、変更せずに理由を提示して人間の判断を仰ぐ。
- 価格・数量は整数（ティック数）。マッチング経路に浮動小数点を導入しない。
- 約定価格は maker（板に載っていた側）の指値。
- 壁時計・実時間に依存しない。到着順は呼び出し順で決まり、内部の連番で管理する。
- 出力に影響する処理で unordered コンテナのイテレーション順に依存しない。
- 乱数は必ずシードを引数で受け取る。グローバル状態の RNG を作らない。ストリームはエージェント単位・コンポーネント単位に分離する（下記「先行固定する設計要件」4）。

## 先行固定する設計要件（Stage 2・4）

未実装だが確定済みの要件。Stage 1–3 の実装で、これらを後から不可能にする選択をしてはならない。
典型的な違反例: 市場が一つだけであることを前提にした静的状態、時刻を持たないイベント、全体で共有する単一の乱数ストリーム、Market を Agent の下位型にすること、エージェントが引数以外から時刻を得ること。

1. **イベントログ（Stage 2）— 再実行なしで TCA が出せること**
   - 注文ごとに、発注時刻（エージェントの決定時刻）・受付時刻（市場到着時刻）・約定時刻を別フィールドで記録する。Stage 1 では発注時刻 = 受付時刻でも、フィールドは分けておく。
   - 受付時点の best bid / best ask（設定により深さ N まで）と連番を記録し、実装ショートフォールの分解（遅延・執行・機会コスト）がログから直接計算できるようにする。
   - ログは追記専用・固定長のバイナリレコード。ログをリプレイした状態のハッシュが元の実行と一致することをテストで保証する。
2. **Market インターフェース（Stage 4）— 注文駆動と時間駆動の両方**
   - 受け口は (a) 注文イベント、(b) 核が予定した時刻イベント（バッチオークションの清算など）の 2 系統。
   - 他市場の状態への読み取りアクセスを持てる（ダークプールの参照価格など）。
   - 配分規則（価格時間優先 / プロラタ）は差し替え可能な部品にする。
   - 時刻は核だけが所有する。市場は自前の時計を持たず、複数市場が同一クロックを共有する。
3. **リプレイ入力（Stage 3–4）— 過去データはエージェントの一種**
   - 過去データのメッセージ列を流し込む ReplayAgent を一級の入力として扱う。
   - 内部のメッセージ語彙は、ITCH の Add / Execute / Cancel / Delete / Replace と LOBSTER の 7 イベント型を表現できる上位集合にする。
   - OrderId は外部から与えられる（現行仕様のまま）。核が付番し直す設計にしない。
4. **乱数と反実仮想（全 Stage）— 同じシードから注文を一つ除いた実行が成立すること**
   - 乱数ストリームはエージェント単位・確率的コンポーネント単位に分離し、マスターシードと ID から決定的に派生させる。全体で一つのストリームを共有しない。
   - 目的: 注文を一つ除いても他エージェントの乱数引きがずれず、二つの実行の差分が板状態の違いだけに起因する状態を保つ。これがインパクト計測（反実仮想）の前提であり、崩すと差分が測定にならない。
5. **Market と Agent の関係（Stage 4）— 同期観測、遅延は配達側に載せる**
   - Market は Agent の下位型にせず、共通の基底クラスも置かない。Market は Agent の契約（起床、注文送出、口座、乱数ストリーム）を持たない。横断機能（状態ハッシュ、ログ）は concept か自由関数で与える。
   - エージェントの観測（板、自分の注文、他市場）は、核が起こした時刻にその時点の状態を直接読む同期読み取りとし、メッセージ往復にしない。理由: (i) Chiarella–Iori、Speculation Game、Plham/PAMS、JPX 系のダークプール研究は全て「現在の状態を見て発注する」モデルで、再現に必要。(ii) Stage 5 で、同一時刻に起こす Python エージェント群に観測を 1 回の呼び出しで配列として渡し、注文を 1 回で受け取るため。往復方式では 1 通ごとに C++/Python 境界を跨ぐ。
   - エージェントは時刻を引数でのみ受け取る（観測時刻 = 起床時刻）。核の時計を直接読まず、自前の時計も持たない。時刻に依存する内部状態の更新（価格履歴、経過時間による減衰など）はこの引数の時刻で行う。
   - 遅延はすべて配達側に載せる。注文は decided_at（観測・決定時刻）に決まり、received_at = decided_at + 到着遅延で配達される（要件 1 のフィールド分離に対応）。約定通知も遅延付きで Agent へ配達する。観測遅延（古い状態を見る）は、起床を早め到着遅延を長くすることに帰着させ、過去の板状態は保持しない。帰着が厳密に成り立つ条件: (a) エージェントが外界に影響する経路がヒープを通る注文・通知だけであること、(b) 起床順の入れ替えが他エージェントの乱数列に影響しないこと（要件 4）、(c) エージェントが時刻を引数以外から得ないこと（前項）。表現できるのは観測と通知が同じ遅延を受ける場合まで。通知が観測より速い状況と、観測遅延の直接表現は Stage 4 の範囲外とする。
   - 時刻の配送経路は核のヒープ一本。イベントは閉じた集合（Agent 起床、Market 時刻イベント、注文配達、Agent への通知、介入）を std::variant で定義し、核が種別ごとに Agent / Market の型付きメンバ関数へ配る。注文配達の中身は要件 3 のメッセージ語彙。どの実体も自前の時計を持たず、ポーリングもしない。
   - ログとの対応: ログは市場側の入力と結果を記録し、ログだけから板状態を再現できる集合とする。注文配達（新規）→ Add + Fill 0 件以上、または Reject。注文配達（取消）→ Cancel、または Reject。Market 時刻イベント → 時刻レコード（Stage 4 で kind を追加）+ Fill 0 件以上。介入 → 介入レコード（同上）。Agent 起床と通知は記録しない（決定論的スケジュールと要件 4 により再現できる）。Stage 2 の共通レイアウトは kind の追加で対応するので、kind を 4 値で閉じない。
   - 同時刻の規則: 同じ時刻に起こされたエージェントは全員同じ状態を観測する。到着遅延は 1 以上。各時刻では配達を連番順に処理してから起床を処理する。これにより Stage 5 の一括観測が逐次呼び出しと同じ結果を与える。核は配達時に received_at > decided_at を検査する（デバッグビルドの assert と、ログ上の不変条件テスト）。板の契約（received_at の単調性のみ）は変えず、Stage 1–3 の直接駆動では decided_at = received_at のままでよい。

## 作業ルール

- 赤 → 緑 → リファクタの順。新しい挙動には先にテストを足す。
- 最適化は禁止（既定）。行う場合は、(1) 参照実装との差分テストが存在し、(2) Google Benchmark の前後比較を diff として提示できる場合のみ。
- 素朴な参照実装（reference book）は最適化後も削除しない。差分テストの土台である。
- 依存ライブラリの追加は提案のみ。無断で FetchContent に追加しない。
- C++20。-Wall -Wextra -Wpedantic（MSVC: /W4）で警告ゼロを維持する。
- 一度の変更は小さく。1 コミット = 1 論点。コミットメッセージは変更理由を一行で。

## エージェントへの委任範囲

- 任せてよい: ビルド設定、テストハーネス、ベンチマークコード、CI、ドキュメント、リファクタ、Stage 3 以降の計測と分析、既存仕様の別実装（差分テストで守られている場合）。
- 任せない（人間が書く）: Stage 1 のマッチングロジック初版、Stage 2 のイベントログ設計の初版、public インターフェースの変更判断。

## ディレクトリ

- include/lobcore/ — 公開ヘッダ（#include <lobcore/...> 形式）
- src/ — 実装
- tests/ — Catch2。仕様のすべて
- bench/ — Google Benchmark（既定 OFF）
- docs/ — 設計文書と context pack
